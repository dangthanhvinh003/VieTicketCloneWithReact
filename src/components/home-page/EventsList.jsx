import React from "react";
import PropTypes from "prop-types";

const EventsList = ({commonClassName}) => {
    return (
        <>
        </>
    );
}

EventsList.propTypes = {
    commonClassName: PropTypes.string.isRequired,
};

export default EventsList;